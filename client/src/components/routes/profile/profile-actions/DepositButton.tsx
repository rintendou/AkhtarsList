import StyledButton from "../../../ui/StyledButton"

const DepositButton = () => {
  return (
    <StyledButton
      buttonText="Deposit"
      onClick={() => {}}
      twClasses="w-32 hover:bg-black"
    />
  )
}

export default DepositButton
