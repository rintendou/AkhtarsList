import StyledButton from "../../../../ui/StyledButton"

const WithdrawButton = () => {
  return (
    <StyledButton
      buttonText="Withdraw"
      onClick={() => {}}
      twClasses="w-32"
      intent="secondary"
    />
  )
}

export default WithdrawButton
