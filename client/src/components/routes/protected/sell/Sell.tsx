import SellActions from "./SellActions"

const Sell = () => {
  return (
    <div className="min-h-screen min-w-full flex flex-col md:flex-row container mx-auto dark:bg-black">
      <SellActions />
    </div>
  )
}

export default Sell
